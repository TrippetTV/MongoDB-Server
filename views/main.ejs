<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="stylesheet" href="css/dimmer.css">
    <link rel="stylesheet" href="css/home.css">

    <title>Document</title>
</head>
<body>
    <div class=".container-fluid">
        <div class="row align-items-center">
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <div class="col">
                    <a class="navbar-brand" href="/">Bookface</a>
                </div>
                <div class="col">
                    <div style="text-align: center">
                        <button class="btn btn-outline-success" id="post-create">Create Post</button>
                    </div>
                </div>
                <div class="col">
                    <div class="d-flex justify-content-end btn-toolbar">
                            <div class="btn-group mr-2">
                                <a href="/create"><button class="btn btn-outline-success">Signup</button></a>
                                <a href="/loginpage"><button class="btn btn-outline-success">Login</button></a>
                            </div>
                        </div>
                </div>
            </nav>
        </div>
    </div>
    <div class="row">
        <div class="col">

        </div>
        <div class="col" id="post-creation">
            <form action="/" method="post" >
                <label for="message"></label>
                <textarea name="message" id="message" cols="50" rows="20" placeholder="What's on your mind?">

                </textarea>
                <input type="submit" class="btn btn-outline-success" value="Post">
            </form>
            <div class="dimmer">
                <span class="exit">&times;</span>
            </div>
        </div>
        <div class="col">

        </div>
    </div>
    <div class="row">
        <div class="col">

        </div>
        <div id="posts" class="col">
            <!-- TODO Make this shit work, for every message in collection, make a card for that message -->
            <% data.forEach(function(e){%>
            <div class="card" style="width: 100%">
                <div class="card-body">
                    <h5 class="card-title"><%=e.from%></h5>
                    <h6 class="card-subtitle mb-2 text-muted">
                        <% let dd = String(e.date.getDate()).padStart(2, '0'); %>
                        <% let mm = String(e.date.getMonth() + 1).padStart(2, '0'); %>
                        <% let yyyy = e.date.getFullYear(); %>

                        <%= dd + '/' + mm + '/' + yyyy %>
                    </h6>
                    <p class="card-text"> <%= e.message %> </p>
                    <% console.log(e.message) %>
                </div>
            </div>
            <% }) %>
        </div>
        <div class="col">

        </div>
    </div>


</body>
</html>
<script>
    let check = false;
    const button = document.querySelector("#post-create")
    const posts = document.querySelector("#post-creation")
    const txt = document.querySelector("#message")
    button.addEventListener("click", (e) => {
        //const dimmer = $('.dimmer');
        if (!check) {
            posts.style.visibility = "visible"
            txt.style.height = "250px"
            check = true
        }
    })
        exit = document.getElementsByClassName("exit")
        exit.addEventListener("onclick", (e) => {
            check = false
            txt.style.height = "0"
            posts.style.visibility = "hidden"

        })
</script>